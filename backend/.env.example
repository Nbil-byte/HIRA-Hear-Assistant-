// Add proper .env files and validation
// backend/.env.example
PORT=3000
CLOUD_BUCKET_NAME=your-bucket-name
GOOGLE_APPLICATION_CREDENTIALS=path/to/serviceaccount.json
MODEL_API_URL=http://localhost:8080

// Add environment validation
if (!process.env.CLOUD_BUCKET_NAME) {
  throw new Error('CLOUD_BUCKET_NAME is required');
}